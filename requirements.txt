# Core runtime dependencies (sufficient to build, train on synthetic data, and test).
torch>=2.0
numpy>=1.23

# Dev / test
pytest>=7.0

# Optional foundation-model backends (install with: pip install -r requirements-foundation.txt)
# transformers>=4.40
# soundfile>=0.12
