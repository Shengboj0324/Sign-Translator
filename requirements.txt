# Core runtime dependencies (sufficient to build, train on synthetic data, and test).
av>=18,<19
torch>=2.13,<2.14
numpy>=2.5,<2.6

# Dev / test
pytest>=9.1,<9.2

# Optional foundation-model backends are listed in requirements-foundation.txt.
